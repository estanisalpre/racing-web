import ScheduleCardComponent from "../../../lib/components/ScheduleCardComponent";

export default function ScheduleComponent() {
  return (
    <section className="schedule-container">
        <h2>CALENDARIO</h2>
        <ScheduleCardComponent/>
    </section>
    
  )
}
